python-dotenv==1.0.1  # For reading environment variables stored in .env file

# LangChain 0.3.x upgrade (Option B)
# Keep all langchain-associated packages aligned on the same major/minor family.
langchain==0.3.1
langchain-community==0.3.1
langchain-openai==0.2.0
langchain-chroma==0.1.6   # Adjusted for 0.3.x compatibility
langchain-text-splitters==0.3.0
langchain-google-genai==1.0.11

unstructured==0.14.4       # Document loading
# Chroma version kept within <0.6 for compatibility (adjust if future constraints change)
chromadb==0.5.7            # Vector store
openai==1.51.0             # For embeddings (locked to 1.x)
requests==2.32.3
tiktoken==0.7.0            # For embeddings
pypdf==4.3.1
sentence-transformers==3.0.1
numpy==1.26.4
pydantic==2.9.1

# API server
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9